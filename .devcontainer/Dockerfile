# Use the main Dockerfile's development stage
FROM ghcr.io/saeloun/miru-web:devcontainer-latest

# The development stage in the main Dockerfile already has everything we need
# Just set the working directory and we're good to go
WORKDIR /app