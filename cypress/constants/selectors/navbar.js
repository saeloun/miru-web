import { dataCy } from "../../support/utils/datacy";

export const navbarSelectors = {
  dashboardTab: dataCy("dashboard-link"),
  timeTrackingTab: dataCy("time-tracking-link"),
  teamTab: dataCy("team-link"),
  clientsTab: dataCy("clients-link"),
  projectsTab: dataCy("projects-link"),
  invoiceTab: dataCy("invoice-link"),
  reportsTab: dataCy("reports-link")
};
