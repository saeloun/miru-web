<div class="max-w-6xl mx-auto px-2 md:px-11 font-manrope">
  <div>
    <div>
      <%= react_component("Profile/Layout", { isAdmin: (current_user.has_role?(:owner, current_company) || current_user.has_role?(:admin, current_company)), company: current_company, userDetails: current_user.attributes }) %>
    </div>
  </div>
</div>
