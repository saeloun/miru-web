<div class="" id="root-page">
  <%= react_component("SpaceUsages/Index", {
      entries: entries,
      isAdmin: is_admin,
      userId: user_id,
      userDepartmentId: user_department_id,
      employees: employees,
      departments: departments,
      spaceCodes: space_codes,
      purposeCodes: purpose_codes,
    }, prerender: false, camelize_props: false) %>
</div>
